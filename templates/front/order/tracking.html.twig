{% extends 'front/layout.html.twig' %}
{% set menu_order_tracking= true %}
{% block body %}
    <div class="site-section">
        <div class="container">
            <table class="table table-bordered">
                <thead>
                <tr>
                    <th>Nombre d'élements</th>
                    <th>Reference achat</th>
                    <th>Status</th>
                    <th>Details</th>
                </tr>
                </thead>
                <tbody>
                {% for order in orders %}
                    <tr>
                        <td class="product-thumbnail">
                            {{ order.items.count }}
                        </td>
                        <td class="product-name">
                            <h2 class="h5 text-black">{{ order.orderRef }}</h2>
                        </td>
                        <td>{{ order.getStateToString() }}</td>
                        <td>
                            <a href="{{ path('order_details', {'id': order.id}) }}"><i class="icon icon-eye"></i></a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
            {{ knp_pagination_render(orders) }}
        </div>
    </div>
{% endblock %}
